#!/bin/bash
# copy this file to "config.sh" and change the following values

username="remote ssh username"
host="remote ssh host"
remote_path="remote/path/where/files/are/being/saved/"
local_path="local/path/where/files/are/being/copied/"
