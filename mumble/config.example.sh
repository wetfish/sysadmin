#!/bin/bash
# copy this file to "config.sh" and change the following values

# The IP and port of the server this script is being run on
CurrentIP=127.0.0.1
CurrentPort=64738

# The IP and port of the server we're connecting to
TargetIP=127.0.0.1
TargetPort=64738
